Precise Plastic Unicorn

Medium

# Wrong reciver address  in function `withdrawAndCall` in contract `GatewayTransferNative.sol`

## Overview : 
function makes the transfer to `address(this)` instead of receivers address . 

## Detail:
In function `withdrawAndCall` in contract `GatewayTransferNative.sol`  there is a reciver address as input which intended to receive the transfer of amount in external chain. 
This function calls the function `withdrawAndCall` in `gatewayZEVM.sol` which also takes reciver address and intended to send the amount to receiver address , that function also calls the function `_withdrawZRC20WithGasLimit` which transfers gas fees and transfer amount to `address(this)` instead of receiver. 

```solidity
    function withdrawAndCall(
        bytes32 externalId,
        bytes memory contractAddress,
        address targetZRC20,
        uint256 outputAmount,
        bytes memory receiver,
        bytes memory message
    ) internal {
        gateway.withdrawAndCall(
            contractAddress,
            outputAmount,
            targetZRC20,
            message,
            CallOptions({
                isArbitraryCall: false,
                gasLimit: gasLimit
            }),
            RevertOptions({
                revertAddress: address(this),
                callOnRevert: true,
                abortAddress: address(0),
                revertMessage: bytes.concat(externalId, receiver),
                onRevertGasLimit: gasLimit
            })
        );
    }
```

```solidity
    function _withdrawZRC20WithGasLimit(uint256 amount, address zrc20, uint256 gasLimit) private returns (uint256) {
        (address gasZRC20, uint256 gasFee) = IZRC20(zrc20).withdrawGasFeeWithGasLimit(gasLimit);
        if (!IZRC20(gasZRC20).transferFrom(msg.sender, PROTOCOL_ADDRESS, gasFee)) {
            revert GasFeeTransferFailed();
        }

        if (!IZRC20(zrc20).transferFrom(msg.sender, address(this), amount)) { 
            revert ZRC20TransferFailed(); // q here we should use recivers address 
        }

        if (!IZRC20(zrc20).burn(amount)) revert ZRC20BurnFailed();

        return gasFee;
    }
```

https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/main/omni-chain-contracts/contracts/GatewayTransferNative.sol#L253

## Impact :

Withdrawals will not reach to the intended person who needs that .

## Recommendation : 

Add the correct address.   

