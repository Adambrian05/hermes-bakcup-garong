Suave Maroon Rook

Medium

# GatewaySend::onRevert does not handle situations where Native token is refunded

### Summary

According to the [Zetachain docs](https://www.zetachain.com/docs/developers/chains/zetachain/#revert-transactions), an `incoming call` revert behaviour occurs as follows 
```solidity
Revert Behavior:

If the amount sent with the original call is enough to cover revert gas fees:

ZetaChain swaps part of the amount into gas tokens (ZRC-20) for the connected chain.
The protocol sends the remaining tokens and revert message to the revertAddress on the connected chain. // <- Here
If callOnRevert is true, the Gateway invokes the onRevert function.
```

The remaining tokens is sent back to the revert address since the contract `GatewaySend` also handles eth deposits, on revert ,Eth will be sent back 

Since  GatewaySend::onRevert does not handle native transfers, the refund will fail or be trapped.

### Root Cause

In [GatewaySend::onRevert ](https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/main/omni-chain-contracts/contracts/GatewaySend.sol#L393)
```solidity
   function onRevert(RevertContext calldata context) external onlyGateway {
        bytes32 externalId = bytes32(context.revertMessage[0:32]);
        address sender = address(uint160(bytes20(context.revertMessage[32:]))); 
        TransferHelper.safeTransfer(context.asset, sender, context.amount);
        
        emit EddyCrossChainRevert(
            externalId,
            context.asset,
            context.amount,
            sender
        );
    
```
it can be seen that it only handles erc20 transfer, not native.
When the refund is for a native asset, `context.asset` is empty, leading to reverts or funds getting stuck

### Internal Pre-conditions

......

### External Pre-conditions

In the [Zetachain gateway contract](https://github.com/zeta-chain/protocol-contracts/blob/a2b6c07814ed108fcba4f2f124757bf45af2f8aa/contracts/evm/GatewayEVM.sol#L123) this function is used for native refunds. The native token is sent back first before the revert call is made.
```solidity
 /// @notice Transfers msg.value to destination contract and executes it's onRevert function.
    /// @dev This function can only be called by the TSS address and it is payable.
    /// @param destination Address to call.
    /// @param data Calldata to pass to the call.
    function executeRevert(
        address destination,
        bytes calldata data,
        RevertContext calldata revertContext
    )
        public
        payable
        nonReentrant
        onlyRole(TSS_ROLE)
        whenNotPaused
    {
        if (destination == address(0)) revert ZeroAddress();
        (bool success,) = destination.call{ value: msg.value }(""); //@audit
        if (!success) revert ExecutionFailed();
        Revertable(destination).onRevert(revertContext);

        emit Reverted(destination, address(0), msg.value, data, revertContext);
    }
```

### Attack Path

...

### Impact

The revert and refund mechanism for native tokens will not work, leading to loss of funds or funds getting trapped.

### PoC

_No response_

### Mitigation

`GatewaySend::onRevert` should be corrected to handle eth refunds by calling `safeTransferETH()` when appropriate.