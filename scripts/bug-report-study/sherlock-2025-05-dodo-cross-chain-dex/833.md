Suave Pewter Bat

High

# Token Validation Issue and prevent user's withdrawnative behaviour. Attacker attacks with DoS vector

### Summary

[In GatewaytransferNative.sol 530.sol]

 function withdrawToNativeChain(
        address zrc20,
        uint256 amount,
        bytes calldata message
    ) external payable 

 if(zrc20 != _ETH_ADDRESS_) {
            require(IZRC20(zrc20).transferFrom(msg.sender, address(this), amount), "INSUFFICIENT ALLOWANCE: TRANSFER FROM FAILED");
        } 

zrc20 is malicious attacker token that attacker mints.

In this Zrc20 token


contract MaliciousIZRC20 is IZRC20 {
    address public transferNative;

    constructor(address _transferNative) {
        victimContract = _transferNative;
    }

    // Minimal storage mapping for balances, just for example
    mapping(address => uint256) public balanceOf;

    // Setup initial balance for testing
    function mint(address to, uint256 amount) external {
        balanceOf[to] += amount;
    }

    // Override transferFrom to re-enter victim contract
    function transferFrom(address from, address to, uint256 amount) external override returns (bool) {
        // Re-enter victim contract's withdrawNative function during transferFrom
        (bool success, ) = victimContract.call(
            abi.encodeWithSignature("withdrawNative(address,uint256)", address(this), 1 ether)
        ); 



### Root Cause

[GatewaytransferNative.sol 530](https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/main/omni-chain-contracts/contracts/GatewayTransferNative.sol#L530)

### Internal Pre-conditions

Attacker calls withdrawNative with malicious token as token address  

### External Pre-conditions

Attacker calls withdrawNative with malicious token as token address  

### Attack Path

1. Attacker calls withdrawNative with malicious token as token address 
2. Attacker contract calls this function continously
3.  other users can't be blocked for this function

### Impact

"Due to a DoS attack with infinite calls to the function, the service will become unreliable."



### PoC

_No response_

### Mitigation

mapping(address => bool) public supportedTokens;
modifier onlySupportedToken(address token) {
    require(supportedTokens[token], "Unsupported token");
    _;
}

   function addSupportedToken(address token) external onlyOwner {
        supportedTokens[token] = true;
    }

function removeSupportedToken(address token) external onlyOwner  {
        supportedTokens[token] = false;
    }

 function withdrawToNativeChain(
        address zrc20,
        uint256 amount,
        bytes calldata message
    ) external payable onlySupportedToken(zrc20) {
}