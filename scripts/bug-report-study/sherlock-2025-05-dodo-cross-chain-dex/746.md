Abundant Mandarin Nuthatch

High

# Incorrect wallet address utilized when a call is reverted/aborted from Zetachain to Destination Chain (`GatewaySend` (A) -> `GatewayCrossChain` -> `GatewaySend` (B))

## Vulnerability Details

In the `GatewayCrossChain` contract, when a user calls `GatewaySend.depositAndCall` (A) -> `GatewayCrossChain.onCall` ->  and designates `GatewaySend` (chain B) as the `targetContract`, if the transaction fails on the ZetaChain, the sequence of events may unfold as follows: ZetaChain → EVM onCall fails → ZetaChain onRevert is triggered. In this case, the `GatewayCrossChain` will also serve as the address for the `revertAddress` (i.e., `GatewayCrossChain`).

If this situation occurs, the `GatewayCrossChain.onRevert` function will be invoked during the second flow from `GatewayCrossChain -> GatewaySend (B)`. While the flow is correct, the problem arises when the `GatewayCrossChain` contract includes the destination chain address of the caller in the `RevertOptions.revertMessage`. This leads to the funds becoming trapped in `GatewayCrossChain`, as the EVM address should not be utilized; instead, the address owned by the user on ZetaChain should be utilized.

A similar issue may also occur in the `GatewayTransferNative` contract.

In `GatewayCrossChain.withdrawAndCall`

```solidity
    function withdrawAndCall(
        bytes32 externalId,
        bytes memory contractAddress,
        address targetZRC20,
        uint256 outputAmount,
        bytes memory receiver,
        bytes memory message
    ) internal {
        gateway.withdrawAndCall(
            contractAddress,
            outputAmount,
            targetZRC20,
            message,
            CallOptions({
                isArbitraryCall: false,
                gasLimit: gasLimit
            }),
            RevertOptions({
                revertAddress: address(this),
                callOnRevert: true,
                abortAddress: address(0),
@>                revertMessage: bytes.concat(externalId, receiver), //@audit receiver address refers to receiver address on destination chain (Ethereum/Solana/Bitcoin)
                onRevertGasLimit: gasLimit
            })
        );
    }
```

In `GatewayCrossChain.onRevert`

```solidity
    function onRevert(RevertContext calldata context) external onlyGateway {
        // 52 bytes = 32 bytes externalId + 20 bytes evmWalletAddress
        bytes32 externalId = bytes32(context.revertMessage[0:32]);
@>        bytes memory walletAddress = context.revertMessage[32:]; //@audit wallet address retrieved is the destination chain wallet address

        if(context.revertMessage.length == 52) {
            address receiver = address(uint160(bytes20(walletAddress)));
@>            TransferHelper.safeTransfer(context.asset, receiver, context.amount); // @audit funds are loss if the address on Ethereum = address on Zetachain

            emit EddyCrossChainRevert(
                externalId,
                context.asset,
                context.amount,
                walletAddress
            );
        } else {
            RefundInfo memory refundInfo = RefundInfo({
                externalId: externalId,
                token: context.asset,
                amount: context.amount,
@>                walletAddress: walletAddress // @audit if not, user cannot claim refund since they might not own the exact destination address on zetachain
            });
            refundInfos[externalId] = refundInfo;
            
            emit EddyCrossChainRefund(
                externalId,
                context.asset,
                context.amount,
                walletAddress
            );
        }
    }
```

In `GatewayCrossChain.claimRefund`

```solidity
function claimRefund(bytes32 externalId) external {
    RefundInfo storage refundInfo = refundInfos[externalId];

    address receiver = msg.sender;
    if(refundInfo.walletAddress.length == 20) {
@>        receiver = address(uint160(bytes20(refundInfo.walletAddress))); //@audit user does not own the destination address on zeta chain and thus this will revert
    }
@>    require(bots[msg.sender] || msg.sender == receiver, "INVALID_CALLER"); //@audit user does not own the destination address on zeta chain and thus this will revert
    require(refundInfo.externalId != "", "REFUND_NOT_EXIST");
    
    TransferHelper.safeTransfer(refundInfo.token, receiver, refundInfo.amount);
    delete refundInfos[externalId];

    emit EddyCrossChainRefundClaimed(
        externalId,
        refundInfo.token,
        refundInfo.amount,
        abi.encodePacked(msg.sender)
    );
}
```

## POC

Consider the following scenario

1. Alice invokes `GatewaySend.depositAndCall` on Ethereum, designating the `GatewayCrossChain` contract on Zetachain as the target contract and wants to send assets to BNB chain
    - Entering `GatewayCrossChain.onCall -> GatewayCrossChain.withdrawAndCall`, it includes the `RevertOptions.revertMessage` formatted as `bytes.concat(externalId, bytes20(0xEthereumAlice))`
2. Her call to `GatewayCrossChain.onCall` to be forwarded to BNB chain results in a revert
3. This triggers the `GatewayCrossChain` to revert to the `onRevert` function 
    - In `onRevert`, the revertMessage generated in `GatewaySend.withdrawAndCall` is forwarded to the `onRevert` function
    - The wallet address extracted is `0xEthereumAlice`, rather than the address corresponding to the user's address on `Zetachain`
    - Consequently, her `refundInfos[externalId]` will store `0xAliceEthereum` as her `walletAddress`
4. When Alice attempts to claim her token refund through `GatewayCrossChain.claimRefund`, she is unable to do so, as she may not possess the `0xEthereumAlice` address on Zetachain

## Root Cause

The address provided  `GatewayCrossChain.withdrawAndCall` is wrong on Zetachain in the `RevertOptions.revertMessage` struct, making it incompatible for claiming refunds on Zetachain.

## Impact

Loss of funds for user.

## Mitigation

Enable the user to specify the receiver address for the refund during calls and concatenate that address into the `revertMessage` instead of using the receiver address on the destination chain.

## LOC

https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/main/omni-chain-contracts/contracts/GatewayCrossChain.sol#L571