Bouncy Taffy Elephant

High

# A user will fail to find a valid pair if the factory uses a different init code hash.

### Summary

https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/main/omni-chain-contracts/contracts/libraries/UniswapV2Library.sol#L33
 A user will fail to find a valid pair if the factory uses a different init code hash.

### Root Cause

passing Uniswap V2’s original init code hash directly is not the best practice

### Internal Pre-conditions

no need

### External Pre-conditions

no need

### Attack Path

no need

### Impact

it uses Uniswap V2’s original init code hash, so  a user will fail to find a valid pair if the factory uses a different init code hash when broken pair address calculation for protocols using clones or different deployments

### PoC

no need

### Mitigation

Make INIT_CODE_HASH a constant parameter or settable in a wrapper context

```solidity
bytes32 internal constant INIT_CODE_HASH = 0x...;
```
