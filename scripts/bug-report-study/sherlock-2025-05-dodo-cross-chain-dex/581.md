Tricky Holographic Cobra

Medium

# Approval not set to 0 after swaps would affect Usdt/usdc

## Summary
When executing cross chain swaps in the protocol it can execute swaps using Uniswap or _doMixSwap. However when executing swaps it gives approval to the token during the swap but it is not reseted after swap this would affect tokens like Usdt/usdc if their is remaining tokens or approval after swap.
## Description
```solidity
    function _doMixSwap(
        bytes memory swapData, 
        uint256 amount, 
        MixSwapParams memory params
    ) internal returns (uint256 outputAmount) {
        if (swapData.length == 0) {
            return amount;
        }

        IZRC20(params.fromToken).approve(DODOApprove, amount);
        return IDODORouteProxy(DODORouteProxy).mixSwap(
            params.fromToken,
            params.toToken,
            params.fromTokenAmount,
            params.expReturnAmount,
            params.minReturnAmount,
            params.mixAdapters,
            params.mixPairs,
            params.assetTo,
            params.directions,
            params.moreInfo,
            params.feeData,
            params.deadline
        );
    }
```
As we can see when executing the swap from the `fromToken` suppose its token like usdt/usdc it would execute the first swap but second swap would revert.
## Impact
Dos of second swaps

## Recommended Mitigation
Set approve to zero after swaps