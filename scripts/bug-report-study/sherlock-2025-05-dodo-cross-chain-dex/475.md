Acrobatic Chartreuse Dolphin

High

# `GatewayCrossChain::withdrawAndCall` sets `abortAddress` to `address(0)`, risking permanent fund loss.

### Summary

The `GatewayCrossChain.withdrawAndCall` function hardcodes the `abortAddress` to `address(0)` in its `RevertOptions`. This disables ZetaChain's `onAbort` fail-safe mechanism, which is the final recovery layer for failed cross-chain transactions.

### Root Cause
The `GatewayCrossChain.withdrawAndCall` function hardcodes the `abortAddress` to `address(0)` in its `RevertOptions`. This disables ZetaChain's `onAbort` fail-safe mechanism, which is the final recovery layer for failed cross-chain transactions.

The code can be seen [here](https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/d4834a468f7dad56b007b4450397289d4f767757/omni-chain-contracts/contracts/GatewayCrossChain.sol#L264)

```solidity
 function withdrawAndCall(
      // ...
    ) internal {
        gateway.withdrawAndCall(
            contractAddress,
            outputAmount,
            targetZRC20,
            message,
            CallOptions({
                isArbitraryCall: false,
                gasLimit: gasLimit
            }),
            RevertOptions({
                revertAddress: address(this),
                callOnRevert: true,
                abortAddress: address(0), // @audit address(0)
                revertMessage: bytes.concat(externalId, receiver),
                onRevertGasLimit: gasLimit
            })
        );
    }
```

### Internal Pre-conditions

`GatewayCrossChain::onCall` is called, which calls `GatewayCrossChain::_handleEvmOrSolanaWithdraw`, which calls `GatewayCrossChain::withdrawAndCall` which succeeds.

### External Pre-conditions

test

### Attack Path

1. A user initiates `GatewayCrossChain::onCall`, which calls `GatewayCrossChain::_handleEvmOrSolanaWithdraw` to withdraw Solana to Solana network.

Do note that in the contest's `README.md`, `SOL.SOL` is a supported asset on ZetaChain.

2. Transaction broadcast fails due to network issues on Solana

3. TSS will attempt to call `onAbort`, but since `abortAddress` was set to `address(0)` in `RevertOptions` in `GatewayCrossChain::withdrawAndCall`, funds are lost

### Impact

User funds are lost.

### PoC

Please refer to attack path.

### Mitigation
In `GatewayCrossChain::withdrawAndCall`

```diff
 function withdrawAndCall(
      // ...
    ) internal {
        gateway.withdrawAndCall(
            contractAddress,
            outputAmount,
            targetZRC20,
            message,
            CallOptions({
                isArbitraryCall: false,
                gasLimit: gasLimit
            }),
            RevertOptions({
                revertAddress: address(this),
                callOnRevert: true,
+               abortAddress: address(this),
-               abortAddress: address(0),
                revertMessage: bytes.concat(externalId, receiver),
                onRevertGasLimit: gasLimit
            })
        );
    }
```