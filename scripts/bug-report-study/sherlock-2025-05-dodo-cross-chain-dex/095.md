Suave Pewter Bat

High

# claimrefund function has reentrancy vulnerability issue. Attackers  steal their balance from the contract

### Summary

  function claimRefund(bytes32 externalId) external {
        RefundInfo storage refundInfo = refundInfos[externalId];

        address receiver = msg.sender;
        if(refundInfo.walletAddress.length == 20) {
            receiver = address(uint160(bytes20(refundInfo.walletAddress)));
        }
        require(bots[msg.sender] || msg.sender == receiver, "INVALID_CALLER");
        require(refundInfo.externalId != "", "REFUND_NOT_EXIST");
        
        TransferHelper.safeTransfer(refundInfo.token, receiver, refundInfo.amount);
        delete refundInfos[externalId];

       This function has reentrancy vulnerability  issue. 
       If the receiver is malicious contract
       onReceive()
      {
             If(IRC20(token).balanceOf(address(GatewayTransferNative))>0)
            {
                    GatewayTransferNative.claimRefund(externalId)
            }

      }


### Root Cause

[GatewayTransferNative.sol 671]
(https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/main/omni-chain-contracts/contracts/GatewayTransferNative.sol#L671) 

### Internal Pre-conditions

when attackers call claimrefund function call

### External Pre-conditions

when attackers call claimrefund function call

### Attack Path

1. Attacker is malicious contract.
2. This contract calls claimRefund function
3. After contract sends token to the contract, the malicious contract calls same function

### Impact

The contract loose their balance. Using this reentrancy issue, Attackers can earn tokens 

### PoC

_No response_

### Mitigation

  delete refundInfos[externalId];
TransferHelper.safeTransfer(refundInfo.token, receiver, refundInfo.amount);
     
