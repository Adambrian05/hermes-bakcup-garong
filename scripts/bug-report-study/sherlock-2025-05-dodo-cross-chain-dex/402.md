Shaggy Strawberry Dinosaur

High

# `GatewaySend`.`onRevert` can't return ether to sender

### Summary

`GatewaySend`.`onRevert` doesn't check `context`.`asset` is not `_ETH_ADDRESS_` or `empty`.
When `context`.`asset` is `_ETH_ADDRESS_` or `empty`, `onRevert` can't transfer ethers to `sender`.
It reverts because `context`.`asset` is not exist.

### Root Cause

[`onRevert`](https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/main/omni-chain-contracts/contracts/GatewaySend.sol#L393-L404) doesn't check `context.asset`.
```solidity
    function onRevert(RevertContext calldata context) external onlyGateway {
        bytes32 externalId = bytes32(context.revertMessage[0:32]);
        address sender = address(uint160(bytes20(context.revertMessage[32:])));
@>      TransferHelper.safeTransfer(context.asset, sender, context.amount);
        
        emit EddyCrossChainRevert(
            externalId,
            context.asset,
            context.amount,
            sender
        );
    }
```
When `context.asset` is `_ETH_ADDRESS_` or `empty`, `asset` is not ERC20, it is `Ether`.
So `onRevert` reverts and causes DOS.
**User can't receive ether.**

### Internal Pre-conditions

.

### External Pre-conditions

.

### Attack Path

.

### Impact

User can't receive ether when `onRevert`.

### PoC

_No response_

### Mitigation

```diff
    function onRevert(RevertContext calldata context) external onlyGateway {
        bytes32 externalId = bytes32(context.revertMessage[0:32]);
        address sender = address(uint160(bytes20(context.revertMessage[32:])));
+       if (context.asset == ETH_ADDRESS_ || context.asset == address(0)) {
+	        payable(sender).transfer(context.amount);
+       } else {
	        TransferHelper.safeTransfer(context.asset, sender, context.amount);        
+       }
        
        emit EddyCrossChainRevert(
            externalId,
            context.asset,
            context.amount,
            sender
        );
    }
```