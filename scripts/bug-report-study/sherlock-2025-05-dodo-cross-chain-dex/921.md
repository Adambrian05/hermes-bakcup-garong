Delightful Carbon Stallion

High

# Non ZEVM cross chain transaction charges no fees

### Summary

Non ZEVM cross chain transaction charges no fees

`GatewaySend::onCall`
```solidity
    function onCall(
        MessageContext calldata /*context*/,
        bytes calldata message
    ) external payable onlyGateway returns (bytes4) {
        (
            bytes32 externalId, 
            uint256 amount, 
            bytes calldata recipient, 
            address fromToken, 
            address toToken, 
            bytes calldata swapData
        ) = decodePackedMessage(message);

        bool fromIsETH = (fromToken == _ETH_ADDRESS_);
        bool toIsETH = (toToken == _ETH_ADDRESS_);
        address evmWalletAddress = address(bytes20(recipient));

        if(!fromIsETH) {
            IERC20(fromToken).transferFrom(msg.sender, address(this), amount);
        }

        uint256 outputAmount;
        if(fromToken == toToken) {
            outputAmount = amount;
        } else {
            outputAmount = _doMixSwap(swapData);
        }

        if(toIsETH) {

            payable(evmWalletAddress).transfer(outputAmount);
        } else {
            //@audit safetransfer pls
            IERC20(toToken).transfer(evmWalletAddress, outputAmount);
        }
        
        emit EddyCrossChainReceive(
            externalId,
            fromToken,
            toToken,
            amount,
            outputAmount,
            evmWalletAddress,
            message
        );

        return "";
    }
```

### Root Cause

When a user is bridging from a non ZEVM chain to another, fees are not charged 

### Internal Pre-conditions

N/A

### External Pre-conditions

N/A

### Attack Path

* user bridges to a non ZEVM chain to another

### Impact

Protocol bares the gas fees

### PoC

_No response_

### Mitigation

Charge fees adequately