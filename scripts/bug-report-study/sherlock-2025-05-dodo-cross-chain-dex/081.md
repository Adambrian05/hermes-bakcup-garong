Jolly Tartan Guppy

Medium

# onAbort will never be executed because abortAddress is set to zero

### Summary

`abortAddress` is incorrectly set to zero address in `GatewayCrossChain.sol` and `GatewayTransferNative.sol`.

As a result, abort flow will never be executed and fund may be lost.

### Root Cause

[File: omni-chain-contracts/contracts/GatewayCrossChain.sol](https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/d4834a468f7dad56b007b4450397289d4f767757/omni-chain-contracts/contracts/GatewayCrossChain.sol#L244-L269)
```solidity
244:     function withdrawAndCall(
245:         bytes32 externalId,
246:         bytes memory contractAddress,
247:         address targetZRC20,
248:         uint256 outputAmount,
249:         bytes memory receiver,
250:         bytes memory message
251:     ) internal {
252:         gateway.withdrawAndCall(
253:             contractAddress,
254:             outputAmount,
255:             targetZRC20,
256:             message,
257:             CallOptions({
258:                 isArbitraryCall: false,
259:                 gasLimit: gasLimit
260:             }),
261:             RevertOptions({
262:                 revertAddress: address(this),
263:                 callOnRevert: true,
264:@>               abortAddress: address(0),
265:                 revertMessage: bytes.concat(externalId, receiver),
266:                 onRevertGasLimit: gasLimit
267:             })
268:         );
269:     }
```

If `abortAddress` is set to zero address, `onAbort` will never be executed:

[File: omni-chain-contracts/node_modules/@zetachain/protocol-contracts/contracts/zevm/GatewayZEVM.sol](https://github.com/sherlock-audit/2025-05-dodo-cross-chain-dex/blob/d4834a468f7dad56b007b4450397289d4f767757/omni-chain-contracts/node_modules/@zetachain/protocol-contracts/contracts/zevm/GatewayZEVM.sol#L448-L459)
```solidity
448:     function executeAbort(
449:         address target,
450:         AbortContext calldata abortContext
451:     )
452:         external
453:         nonReentrant
454:         onlyProtocol
455:         whenNotPaused
456:     {
457:@>       if (target == address(0)) revert ZeroAddress();
458:         Abortable(target).onAbort(abortContext);
459:     }
```


### Internal Pre-conditions

- `onRevert` function reverts in `GatewayCrossChain.sol`
- `onRevert` function reverts in `GatewayNativeTransfer.sol`

### External Pre-conditions

N/A

### Attack Path

N/A

### Impact

User's fund will be stuck in universal contract and never be accounted.

Thus, user will face fund loss.

### PoC

_No response_

### Mitigation

Set `abortAddress` to `address(this)`