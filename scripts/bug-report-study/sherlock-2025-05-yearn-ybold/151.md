Plain Bronze Walrus

High

# there is lack of check regarding baseFee in _tend() with maxGasPriceToTend.

### Summary

In Strategy.sol , maxGasPriceToTend = 200 * 1e9 is set in constructor. And there is setter function too regarding it.
the main purpose of this is , Max base fee (in gwei) for a tend.

And contract lacks to check this in tend functionallity only. right now this is checked in tendtrigger functionalllity.
it is checking that basefee of the block should be less than maxtendfee, if in a particular block conidtion suceeds and returns true. but condition can arise that , after seeing this when tend is called block base fee can be skyrocketed due to demand. 

And at that time much gas can be used , and the purpose for which maxtendfee is added, will not be succeeded.

### Root Cause

lack of check in tend functionallity which is there in tend trigger. 
https://github.com/sherlock-audit/2025-05-yearn-ybold/blob/main/yv3-liquityv2-sp-strategy/src/Strategy.sol#L260C1-L260C50

### Internal Pre-conditions

N/A

### External Pre-conditions

sudden increase in gas fee, could result in much usage of gas , then it actually should be . Also here diff chain are used, so maxtendfee also should be diff and should be set accordingly other wise more than expected gas will be used. also once it should be checked that block.basefee is available on all chains which is to be deployed , otherwise it can throw error too

### Attack Path

tend trigger is called, and in that block suppose condition would succeeded, so it will return true.

but when tend is called , it can be next block and at that time basefee of block would be higher and this can result into much gas usage. And tend functionallity time sensitive as it taking care of price. so proper check should be there.


### Impact

more than expected gas would be used 

### PoC

_No response_

### Mitigation

check should be there in _tend() too