Bent Cherry Cougar

High

# Missing Fee Recipient Role Assignment Causes Harvests to Revert, Bricking Protocol Functionality

### Summary

The `LV2SPStakerStrategy` fails to configure itself as the `feeRecipient` or `feeManager` in the `Accountant` contract. As a result, calls to `ACCOUNTANT.distribute()` during harvests **revert indefinitely**, blocking reward distribution and rendering the strategy non-functional. 



### Root Cause

The staker does not claim the `feeRecipient` or `feeManager` role in the `Accountant` during initialization; it is neither the `feeRecipient` nor the `feeManager`.

```solidity
    function _harvestAndReport() internal override returns (uint256 _totalAssets) {
        ACCOUNTANT.distribute(address(asset)); // Claim yBOLD

        // Return total balance
        _totalAssets = asset.balanceOf(address(this));
    }

```


```solidity
    function distribute(address token, uint256 amount) public virtual onlyFeeManagerOrRecipient {
        ERC20(token).safeTransfer(feeRecipient, amount);

        emit DistributeRewards(token, amount);
    }
```
https://github.com/sherlock-audit/2025-05-yearn-ybold/blob/main/yv3-liquityv2-sp-strategy/src/periphery/Accountant.sol#L380-L384

### Internal Pre-conditions

none

### External Pre-conditions

none

### Attack Path

none

### Impact

Without the required role, `_harvestAndReport` fails, halting all reward distribution.  

### PoC

_No response_

### Mitigation

_No response_