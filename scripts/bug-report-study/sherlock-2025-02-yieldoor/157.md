Prehistoric Cobalt Aphid

Medium

# `Leverager::_calculateTokenValues()` does not revert if no price feed exists, leading to instant liquidations

### Summary

[Leverager::_calculateTokenValues()](https://github.com/sherlock-audit/2025-02-yieldoor/blob/main/yieldoor/src/Leverager.sol#L424) returns 0 whenever no price feed is set, instead of reverting. Thus, after a user opens a position, if no price feed is set anymore for this position, even if for a brief moment, all positions can be liquidated, as user [collateral](https://github.com/sherlock-audit/2025-02-yieldoor/blob/main/yieldoor/src/Leverager.sol#L399) would be 0.

### Root Cause

In `Leverager:424`, it does not revert if no price feed is set.

### Internal Pre-conditions

Price feed is not set.

### External Pre-conditions

None.

### Attack Path

1. User opens position (price feed still exists).
2. Price feed is temporarily disabled.
3. User is instantly liquidated.

### Impact

User is instantly liquidated.

### PoC

It never checks if at least one of the price feeds is active, as per the comment
> /// @dev Requires at least one of them to have set a trustworthy pricefeed.
```solidity
function _calculateTokenValues(address token0, address token1, uint256 amount0, uint256 amount1, uint256 price)
    internal
    view
    returns (uint256 usdValue)
{
    uint256 chPrice0;
    uint256 chPrice1;
    uint256 decimals0 = 10 ** ERC20(token0).decimals();
    uint256 decimals1 = 10 ** ERC20(token1).decimals();
    if (IPriceFeed(pricefeed).hasPriceFeed(token0)) {
        chPrice0 = IPriceFeed(pricefeed).getPrice(token0);
        usdValue += amount0 * chPrice0 / decimals0;
    }
    if (IPriceFeed(pricefeed).hasPriceFeed(token1)) {
        chPrice1 = IPriceFeed(pricefeed).getPrice(token1);
        usdValue += amount1 * chPrice1 / decimals1;
    }

    // If chPrice0 is 0, it means token0 doesnt have a pricefeed.
    // Therefore, convert it token1 and from there, convert it to USD
    if (chPrice0 == 0) {
        usdValue += (amount0 * price / PRECISION) * chPrice1 / decimals1;
    } else if (chPrice1 == 0) {
        usdValue += amount1 * PRECISION / price * chPrice0 / decimals0;
    }

    return usdValue;
}
```

### Mitigation

Revert if no price feed is set.