Gentle Amber Mule

Medium

# The `LendingPool::setBorrowingRateConfig` function does not call `updateState` and `updateInterestRates`, which results in incorrect accounting and loss of funds

### Summary
The `setBorrowingRateConfig` function in the `LendingPool` contract does not timely update the borrowing rate state, resulting in incorrect calculations after the function is invoked.

### Root Cause
The root cause of the issue is that the [setBorrowingRateConfig](https://github.com/sherlock-audit/2025-02-yieldoor/blob/b5a0f779dce4236b02665606adb610099451a51a/yieldoor/src/LendingPool.sol#L348-L358) function does not invoke any state updates after it is called. Specifically, the current borrowing rate (reserve.currentBorrowingRate) is not updated, leading to incorrect accounting.

```solidity
    /// @notice Sets the BorrowingRateConfig for an asset
    function setBorrowingRateConfig(
        address asset,
        uint16 utilizationA,
        uint16 borrowingRateA,
        uint16 utilizationB,
        uint16 borrowingRateB,
        uint16 maxBorrowingRate
    ) public onlyOwner notPaused {
        DataTypes.ReserveData storage reserve = reserves[asset];
        setBorrowingRateConfig(reserve, utilizationA, borrowingRateA, utilizationB, borrowingRateB, maxBorrowingRate);
    }

    function setBorrowingRateConfig(
        DataTypes.ReserveData storage reserve,
        uint16 utilizationA,
        uint16 borrowingRateA,
        uint16 utilizationB,
        uint16 borrowingRateB,
        uint16 maxBorrowingRate
    ) internal {
        // (0%, 0%) -> (utilizationA, borrowingRateA) -> (utilizationB, borrowingRateB) -> (100%, maxBorrowingRate)
        reserve.borrowingRateConfig.utilizationA = PRECISION * utilizationA / 10_000;

        reserve.borrowingRateConfig.borrowingRateA = PRECISION * borrowingRateA / 10_000;

        reserve.borrowingRateConfig.utilizationB = PRECISION * utilizationB / 10_000;

        reserve.borrowingRateConfig.borrowingRateB = PRECISION * borrowingRateB / 10_000;

        reserve.borrowingRateConfig.maxBorrowingRate = PRECISION * maxBorrowingRate / 10_000;
    }
```

### Internal pre-conditions
N/A

### External pre-conditions
N/A

### Attack Path
The owner changes the borrowing rate config.

### Impact
Calculations related to the borrowing rate becomes incorrect. It causes loss of funds.

### PoC
N/A

### Mitigation
It is recommended to change the code as follows:
```diff
    function setBorrowingRateConfig( /
        DataTypes.ReserveData storage reserve,
        uint16 utilizationA,
        uint16 borrowingRateA,
        uint16 utilizationB,
        uint16 borrowingRateB,
        uint16 maxBorrowingRate
    ) internal {
+       reserve.updateState();

        // (0%, 0%) -> (utilizationA, borrowingRateA) -> (utilizationB, borrowingRateB) -> (100%, maxBorrowingRate)
        reserve.borrowingRateConfig.utilizationA = PRECISION * utilizationA / 10_000;

        reserve.borrowingRateConfig.borrowingRateA = PRECISION * borrowingRateA / 10_000;

        reserve.borrowingRateConfig.utilizationB = PRECISION * utilizationB / 10_000;

        reserve.borrowingRateConfig.borrowingRateB = PRECISION * borrowingRateB / 10_000;

        reserve.borrowingRateConfig.maxBorrowingRate = PRECISION * maxBorrowingRate / 10_000;

+       reserve.updateInterestRates();        
    }
```