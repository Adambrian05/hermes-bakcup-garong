Round Golden Cow

Medium

# The `depositFee` of the vault is not minted to a fee recipient

### Summary

The vault will charge a deposit fee when the `depositFee > 0`. However the deposit shares didn't mint to a fee recipient. In that way, the fee charged will be distributed to users who holds the shares of vault since the convert ration of shares to assets will be larger, which also includes the current user who should be charged.

To some degree, the mechanism of deposit fees is partial invalid since the current depositing user who should be charged can also benefit from the deposit fee.

[Vault](https://github.com/sherlock-audit/2025-02-yieldoor/blob/main/yieldoor/src/Vault.sol#L61-L70)
```solidity
        if (depositFee > 0) shares -= (shares * depositFee) / 10_000;

        require(shares > 0, "shares cant be 0");
        require(depositAmount0 >= amount0Min, "slippage0");
        require(depositAmount1 >= amount1Min, "slippage1");

        if (totalSupply() == 0) _mint(BURN, 1000);
        _mint(msg.sender, shares);

        return (shares, depositAmount0, depositAmount1);
```

### Root Cause

The `depositFee` of the vault is not minted to a fee recipient.


### Internal Pre-conditions

N/A

### External Pre-conditions

N/A

### Attack Path

N/A

### Impact

To some degree, the mechanism of deposit fees is partial invalid since the current depositing user who should be charged can also benefit from the deposit fee.


### PoC

_No response_

### Mitigation

Mint the `depositFee` of the vault to a fee recipient