Sharp Fossilized Cormorant

Medium

# a disabled reserve should not be able to accept deposits

### Summary

missing check in `LendingPool.deposit()` to stop deposits into disabled reserves.

https://github.com/sherlock-audit/2025-02-yieldoor/blob/main/yieldoor/src/LendingPool.sol#L104-L132

### Root Cause

`LendingPool.deposit()` lacks check to ensure disabled reserves cant get deposited into.

```solidity
    function _deposit(address asset, uint256 amount, address onBehalfOf) internal returns (uint256 yTokenAmount) {
        DataTypes.ReserveData storage reserve = getReserve(asset);
        require(!reserve.getFrozen(), "reserve frozen");//@audit-issue a disabled reserve should not be able to accept deposits too
        // update states
        reserve.updateState();

        // validate

```

This missing check in  `LendingPool.deposit()` undermines the admins ability to disable reserves

### Internal Pre-conditions

not needed.

### External Pre-conditions

not needed.

### Attack Path

not needed.

### Impact

missing check in  `LendingPool.deposit()` undermines the admins ability to disable a reserve because disabled reserves shouldn't be able to get deposited into.



### PoC

_No response_

### Mitigation

Add a check in lendingPool's deposit function to ensure that disabled reserves can't get deposited into.