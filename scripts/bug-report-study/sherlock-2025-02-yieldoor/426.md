Round Felt Grasshopper

Medium

# First depositor can set up a bad ratio

### Summary

The first depositor is responsible for setting up the ratio that all depositors should follow.

### [Root Cause](https://github.com/sherlock-audit/2025-02-yieldoor/blob/main/yieldoor/src/Vault.sol#L129C7-L132C38)

The first depositor is responsible for setting up the ratio:
```solidity
 function _calcDeposit(uint256 bal0, uint256 bal1, uint256 depositAmount0, uint256 depositAmount1)
        internal
        view
        returns (uint256 shares, uint256 amount0, uint256 amount1)
    {
        uint256 totalSupply = totalSupply();

        // If total supply > 0, vault can't be empty
        assert(totalSupply == 0 || bal0 > 0 || bal1 > 0);

        if (totalSupply == 0) {
            // For first deposit, just use the amounts desired
            amount0 = depositAmount0;
            amount1 = depositAmount1;
            shares = (amount0 > amount1 ? amount0 : amount1) - (1000);
```
Furthermore, all other depositors shall follow this ratio:
```solidity
   } else {
            uint256 cross0 = depositAmount0 * bal1;
            uint256 cross1 = depositAmount1 * bal0;
            uint256 cross = cross0 > cross1 ? cross1 : cross0;
            require(cross > 0, "cross");

            // Round up amounts
            amount0 = (cross - 1) / bal1 + 1;
            amount1 = (cross - 1) / bal0 + 1;
            shares = cross * totalSupply / bal0 / bal1;
        }
```
As a result first depositor may set up an unflavorable ratio that will cause some liquidity to be unable to earn yield

### Internal Pre-conditions

N/A

### External Pre-conditions

n/a

### Attack Path

n/a

### Impact

Users may miss out on revenue due to inactive liquidity

### PoC

n/a

### Mitigation

First depositor shall be a trusted actor