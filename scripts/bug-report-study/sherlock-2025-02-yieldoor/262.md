Sharp Fossilized Cormorant

Medium

# a disabled reserve should not be able to accept redeem() calls

### Summary

`LendingPool.redeem()` fails to check and ensure the reserve of the asset being redeemed to isn't disabled.

https://github.com/sherlock-audit/2025-02-yieldoor/blob/main/yieldoor/src/LendingPool.sol#L83-L101

### Root Cause

```solidity
    function redeem(address underlyingAsset, uint256 yAssetAmount, address to)
        public
        notPaused
        nonReentrant
        returns (uint256)
    {
    //@audit-issue a disabled reserve should not be able to accept redeem() calls
        DataTypes.ReserveData storage reserve = getReserve(underlyingAsset);

        if (yAssetAmount == type(uint256).max) {
            yAssetAmount = IyToken(reserve.yTokenAddress).balanceOf(_msgSender());
        }
        // transfer yTokens to this contract
        IERC20(reserve.yTokenAddress).safeTransferFrom(msg.sender, address(this), yAssetAmount);

        // calculate underlying tokens using yTokens
        uint256 underlyingTokenAmount = _redeem(underlyingAsset, yAssetAmount, to);

        return (underlyingTokenAmount);
    }
```

Missing check to ensure assets of disabled reserves aren't redeemable in `LendingPool.redeem()` compromises the ability of admins to disable a reserve and stop it from being used.

### Internal Pre-conditions

_Not_needed

Natural Occurring bug

### External Pre-conditions

_Not_needed

Natural Occurring bug

### Attack Path

_Not_needed

Natural Occurring bug

### Impact

Missing check to ensure assets of disabled reserves aren't redeemable in `LendingPool.redeem()`, this compromises the ability of admins to disable a reserve and stop it from being used.

### PoC

_No response_

### Mitigation

add a check to ensure only assets of active reserves can be redeemed via `LendingPool.redeem()`